# Uniswap Routing API - V3 Only Configuration for Immutable zkEVM Testnet
# This is a simplified configuration for single-chain V3-only deployment

# ============================================
# REQUIRED: Core Configuration
# ============================================
# Needed to authenticate to the Labrys github package repository
GITHUB_PACKAGE_TOKEN=ghp_
# RPC endpoint for Immutable zkEVM testnet
ZK_EVM_TESTNET_RPC=https://rpc.testnet.immutable.com

# V3 subgraph URL (your deployed subgraph)
V3_SUBGRAPH_URL=https://your-subgraph-url/graphql

# Application version
VERSION=1

# Node.js options (for source maps)
NODE_OPTIONS=--enable-source-maps

# ============================================
# OPTIONAL: DynamoDB Caching (Recommended)
# ============================================

# Routes cache tables
ROUTES_TABLE_NAME=RoutesDB
CACHED_ROUTES_TABLE_NAME=RouteCachingDB
ROUTES_CACHING_REQUEST_FLAG_TABLE_NAME=RoutesDbCacheReqFlagDB

# V3 pools cache table
CACHED_V3_POOLS_TABLE_NAME=V3PoolsCachingDB

# ============================================
# OPTIONAL: S3 Caching
# ============================================

# S3 buckets for pool and token list caching (optional)
# POOL_CACHE_BUCKET_3=your-pool-cache-bucket
# POOL_CACHE_GZIP_KEY=poolCache.json.gz
# TOKEN_LIST_CACHE_BUCKET=your-token-list-bucket

# ============================================
# OPTIONAL: Lambda Configuration
# ============================================

# AWS Lambda function name (for caching intent)
# AWS_LAMBDA_FUNCTION_NAME=routing-api-lambda
# CACHING_ROUTING_LAMBDA_FUNCTION_NAME=routing-api-caching-lambda

# ============================================
# OPTIONAL: Debug & Development
# ============================================

# Secret for enabling debug routing config
UNICORN_SECRET=your-random-secret-string

# AWS Region
AWS_REGION=ap-southeast-2
